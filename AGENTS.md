# AGENTS.md

> Auto-generated by AMAGI Core. Do not edit manually.

<!-- AMAGI-VAULT-PROJECT:BEGIN (Amagi Core 管理，勿手改) -->
## 知識庫指針
本專案知識庫：vault `projects/amagi-core/`（邏輯位置，非絕對路徑）。
- 接任務前**先讀該處 `handoff.md`（當前狀態活頁，最新進度）**，再讀 `index.md`、`knowledge/`、近期 `reports/` 恢復脈絡。
- vault 根的本機路徑見全局指針（`~/.codex/AGENTS.md` 的 AMAGI-VAULT 區塊）。
- 產出規範/決策/規格/報告一律寫入該 vault 路徑（依 type 分流），不寫絕對機器路徑。

### 本專案記憶（已內聯，開場即視為已知；需細節再讀 vault `projects/amagi-core/agent/memory/<檔>`）
（尚無）
<!-- AMAGI-VAULT-PROJECT:END -->

## 開發工作流（依全域 doctrine）
本專案依「標準開發工作流」（全域 `~/.claude/CLAUDE.md`／`~/.codex/AGENTS.md`，開場自動載入）：先分析、制訂計畫 → 拆步驟、逐步執行、每步留證據、不跳步不併步 → 自我驗證（完成＝有客觀證據）→ 4.5 交叉審查（含完成度稽核）→ commit 預覽。
步驟與結果軌跡寫入 `.amagi/workflow-state.md`（進行中），完成後歸檔 `.amagi/history/`。
